# Node.js
node_modules/
package-lock.json

# 절대 커밋하면 안 되는 시크릿 관련 (혹시 실수로 생성되더라도 방어)
auth.json
auth_encoded.txt
*.env
.env.local
.env.*.local

# AlphaFold 실행 결과물 (용량 크고, 매번 재생성되는 파일이라 git에 불필요)
output/
outputs/
*.pdb
*.pkl
msas/

# AlphaFold 가중치/DB (repo에 절대 넣으면 안 됨 — 수백GB~TB급)
params/
*.npz
*.tar

# OS/에디터 잡파일
.DS_Store
Thumbs.db
.vscode/
*.swp

# Python (혹시 나중에 분석 스크립트 추가 시 대비)
__pycache__/
*.pyc
.ipynb_checkpoints/