# 쉘 스크립트는 항상 LF로 강제 (리눅스 컨테이너에서 실행되므로)
*.sh text eol=lf

# 설정 파일들도 LF로 통일 (일관성)
*.json text eol=lf
*.jsonc text eol=lf
*.py text eol=lf

# Dockerfile도 LF
Dockerfile text eol=lf
docker/Dockerfile text eol=lf